const React = require('react');


const Sight = ({pokemon}) => {
  return(
    <div>
      <h1>PokeDex</h1>
        {/* <h2>{pokemon.name}'s Poke Profile </h2>
        <img src={`${pokemon.img}.jpg`} alt={pokemon.name} /> */}
    </div>
  )
}

module.export = Sight; 